import { Doc } from '../../../types';


const doc: Doc = {
  args: ['(required_players)'],

  description: 'Notifies everyone from the list of LoL players that are not in your voice channel.'
  + ' You can also pass the number of *required_players*',
};

export default doc;
